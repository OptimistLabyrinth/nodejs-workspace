const OFF = 'off'
const WARN = 'warn'
const ERROR = 'error'
const defaultLevelLogic = WARN
const defaultLevelStyle = WARN

module.exports = {
  OFF,
  WARN,
  ERROR,
  defaultLevelLogic,
  defaultLevelStyle,
}
